project(libbase)

set(TARGET_NAME libbase)

set(HEADER_FILE
    Singleton.h
    Queue.h
    List.h
    Array.h
    QList.h
)

Set(SOURCE_FILE
)

#add_library(${TARGET_NAME} ${HEADER_FILE} ${SOURCE_FILE})
#set_target_properties(${TARGET_NAME} PROPERTIES OUTPUT_NAME "base")
